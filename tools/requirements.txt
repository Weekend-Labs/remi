# Art pipeline dependencies for tools/*.py — see docs/DESIGN-YOUR-OWN-BUDDY.md
# Install with:  python3 -m pip install -r tools/requirements.txt
Pillow>=10.0      # image load/save, flood-fill cutout, icon compositing
numpy>=1.24       # array masking for magenta keying (segment_sheet, make_icon)
scipy>=1.10       # connected-component labeling of sprite blobs (segment_sheet)
